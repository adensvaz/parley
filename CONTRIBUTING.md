# Contributing

Internal monorepo. Branch off `main`, open a PR, keep CI green, get a CODEOWNER review.
Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`). `pnpm typecheck && pnpm test` before pushing.
